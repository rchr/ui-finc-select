import React from 'react';
import _ from 'lodash';
import PropTypes from 'prop-types';

class FilterView extends React.Component {
  static manifest = Object.freeze({
    query: {},
  });

  render() {
    return (
      'hallo filter details'
    );
  }
}

export default FilterView;
