import React from 'react';

class CollectionsView extends React.Component {
  render() {
    return (
      <React.Fragment>
        here the connected collections will be shown
      </React.Fragment>
    );
  }
}

export default CollectionsView;
